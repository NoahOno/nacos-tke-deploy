apiVersion: v1
kind: Service
metadata:
  name: nacos-mcp-router
  namespace: nacos
  labels:
    app: nacos-mcp-router
spec:
  type: NodePort
  selector:
    app: nacos-mcp-router
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
